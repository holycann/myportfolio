export interface ProjectItem {
  title: string;
  githubUrl: string;
  webUrl: string;
  description: string;
  features: string[];
  techStack: string[];
  imageUrl?: string; // Optional image for the project
} 